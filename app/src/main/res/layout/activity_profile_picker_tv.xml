<?xml version="1.0" encoding="utf-8"?>
<!-- 2026-06-03 v4 : LinearLayout vertical centré (au lieu de chain Constraint
     qui ne respectait pas le bias). Tout le bloc title+RV+indicator centré
     entre top parent et btn_manage. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- 2026-06-09 : bg par défaut en ImageView (au lieu de @drawable sur le
         root) → permet de le cacher quand l'user a un wallpaper perso. -->
    <ImageView
        android:id="@+id/iv_profile_picker_default_bg"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_app_black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:ignore="ContentDescription" />

    <LinearLayout
        android:id="@+id/ll_picker_block"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btn_manage_profiles"
        app:layout_constraintVertical_bias="0.55">

        <TextView
            android:id="@+id/tv_profile_picker_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="64dp"
            android:layout_marginEnd="64dp"
            android:gravity="center"
            android:text="Qui regarde ?"
            android:textColor="#FFFFFF"
            android:textSize="42sp"
            android:textStyle="bold" />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp">

            <ImageView
                android:id="@+id/btn_profile_prev"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_gravity="center_vertical|start"
                android:layout_marginStart="80dp"
                android:src="@drawable/ic_chevron_left"
                android:contentDescription="Profil précédent"
                android:padding="14dp"
                android:background="@null"
                android:focusable="true"
                android:clickable="true"
                app:tint="#FFFFFFFF" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rv_profiles"
                android:layout_width="match_parent"
                android:layout_height="290dp"
                android:layout_marginHorizontal="200dp"
                android:clipToPadding="false"
                android:orientation="horizontal"
                android:focusable="true"
                android:focusableInTouchMode="false"
                android:descendantFocusability="afterDescendants"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                tools:itemCount="3"
                tools:listitem="@layout/item_profile_tv" />

            <ImageView
                android:id="@+id/btn_profile_next"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_gravity="center_vertical|end"
                android:layout_marginEnd="80dp"
                android:src="@drawable/ic_chevron_right"
                android:contentDescription="Profil suivant"
                android:padding="14dp"
                android:background="@null"
                android:focusable="true"
                android:clickable="true"
                app:tint="#FFFFFFFF" />
        </FrameLayout>

        <TextView
            android:id="@+id/tv_page_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textColor="#CCCCCC"
            android:textSize="18sp"
            tools:text="1 / 3" />

    </LinearLayout>

    <Button
        android:id="@+id/btn_manage_profiles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:background="@drawable/bg_tv_button_focus"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:focusable="true"
        android:clickable="true"
        android:text="Gérer les profils"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
