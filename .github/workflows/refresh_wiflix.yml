name: refresh_wiflix

on:
  schedule:
    - cron: '17 */6 * * *'   # toutes les 6h
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: refresh_wiflix
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install patchright (Playwright anti-CF) + xvfb
        run: |
          pip install --quiet patchright
          python -m patchright install chromium --with-deps
          sudo apt-get install -y -qq xvfb

      - name: Run Wiflix scraper (WARP + Playwright headed sous xvfb)
        run: xvfb-run -a --server-args="-screen 0 1280x1024x24" python scripts/refresh_wiflix.py

      - name: Commit wiflix.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add provider_web/wiflix.json
          if git diff --cached --quiet; then
            echo "Pas de changement."
          else
            git commit -m "auto: refresh wiflix.json (WARP)"
            git push
          fi
